---
const { innerBlocks = [], Block } = Astro.props;
---
<ul class="list">
  {innerBlocks.length > 0 && innerBlocks.map((block: any) => (
    <Block component={block.name} attributes={block.attributes} innerBlocks={block.innerBlocks || null} />
  ))}
</ul>
